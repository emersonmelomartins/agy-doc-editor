# Backend API URL
VITE_API_URL=http://127.0.0.1:3333/api

# Optional fallback for PDF->DOCX conversion endpoint
# true: web can call /api/convert/pdf-to-docx when extraction quality is low
# requires LibreOffice (soffice) installed on API host
VITE_ENABLE_LIBREOFFICE_FALLBACK=false
