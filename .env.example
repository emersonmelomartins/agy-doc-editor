# Root env base (copy values to each app env file)
# API
PORT=3333
HOST=0.0.0.0
SQLITE_PATH=./docsi.db

# Supabase sync (optional)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# WEB
VITE_API_URL=http://127.0.0.1:3333/api
# true = tenta conversao PDF->DOCX no backend via LibreOffice
VITE_ENABLE_LIBREOFFICE_FALLBACK=false
